v3.1 — Feature release: managed filters, consolidation tabs, simulation mode, bug fixes

UX-57: Managed-package filtering (NamespacePrefix-based IsManaged flag,
  "Include managed packages" toggle on all actionable analysis lists).
UX-58: New "Mergable PermSets" tab — near-duplicate pairs (Jaccard >= 0.85)
  and strict subsets in two-column layout with grant-level compare pane.
UX-59: New "Duplicate PermSets" tab — exact-duplicate clusters via
  union-find with pair-picker for 3+ member clusters.
UX-60: Last assignment activity on Orphaned PermSets via SetupAuditTrail
  Query 14 with 6-month retention caveat.
UX-61: Dormant / Empty / Single-User Profile analysis in Migration tab
  with configurable dormancy threshold slider.
UX-62: "Run Delete Impact Analysis" cross-link on orphaned PermSet rows.
UX-63: Simulation Mode (what-if) — add/remove PermSet assignments and
  delete PermSets with cascading propagation across the entire app.
  Yellow simulation banner, named .pebundle export with baseline slot,
  simulation bundle import/round-trip.
BUG-18: Dead Fields confirmed correct after BUG-19 data fix (no code change).
BUG-20: Migration tab hang fixed — async chunked generator for profile
  clustering (requestIdleCallback pattern).
BUG-21: Redundancy tab freeze on managed-filter toggle fixed — useMemo
  for subsetsAnnotated computation.
BUG-22: consolidationMarks now persisted to separate IDB key; survives
  page reload and bundle re-import.
Banner fix: Load All Computations rewritten as async with yield-frame
  between phases so progress banners paint to DOM.
consolidationMarks state added for merge/consolidation tracking;
  round-trips through IDB cache, .pebundle export, and import.
