node_modules/
npm-debug.log*
yarn-debug.log*
.DS_Store
.env
.env.local

# Local pebundle outputs from the weekly Cowork scheduled task. These contain
# real Salesforce data and must never be committed.
bundles/
*.pebundle

# Session scratch (only relevant when working from a Claude Cowork session)
/sessions/

# Build / dist
dist/
build/
*.tgz
