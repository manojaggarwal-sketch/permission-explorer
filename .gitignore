node_modules/
npm-debug.log*
yarn-debug.log*
.DS_Store
.env
.env.local

# Raw un-scrubbed SOQL exports. NEVER commit — these contain real employee
# names, emails, and Salesforce Ids. The bake script reads from here and
# emits scrubbed output into data/snapshot.json, which IS committed.
mcp-seed/raw/
mcp-seed/scrubbed/

# Session scratch (only relevant when working from a Claude Cowork session)
/sessions/

# Build / dist
dist/
build/
*.tgz
