{
  "name": "permission-explorer",
  "version": "2.9.0",
  "private": true,
  "description": "Salesforce permission explorer — single-file React artifact backed by a GitHub-hosted demo snapshot.",
  "scripts": {
    "bake": "node scripts/bake-snapshot.js",
    "verify": "node scripts/bake-snapshot.js --verify"
  },
  "engines": {
    "node": ">=18"
  }
}
