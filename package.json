{
  "name": "permission-explorer",
  "version": "3.6.0",
  "private": true,
  "description": "Salesforce permission explorer — single-file React artifact. CSV-only data flow with IndexedDB pebundle cache.",
  "scripts": {},
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
